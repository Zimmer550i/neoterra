import 'package:flutter/material.dart';

class ProfessionalBookingRequest extends StatelessWidget {
  const ProfessionalBookingRequest({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Padding(
        padding: EdgeInsetsGeometry.symmetric(horizontal: 20),
        child: Column(children: [
          Spacer(),
          
          Spacer(),
        ]),
      ),
    );
  }
}
