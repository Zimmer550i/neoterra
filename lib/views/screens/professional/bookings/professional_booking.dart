import 'package:flutter/material.dart';

class ProfessionalBooking extends StatelessWidget {
  const ProfessionalBooking({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}